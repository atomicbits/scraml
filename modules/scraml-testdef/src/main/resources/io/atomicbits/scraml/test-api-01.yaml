#%RAML 0.8
---
title: test01
version: v1

/rest/user/{userid}:
  uriParameters:
    userid:
      description: The user's id.
      type: string
      required: true
  get:
    responses:
      200:
        body:
          application/json:
            schema: |
              {
                 "": "http://json-schema.org/draft-03/schema",
                 "name": "Joe",
                 "surname": "Smith",
                 "id": 12
              }
